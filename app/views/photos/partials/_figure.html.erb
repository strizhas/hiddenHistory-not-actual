
	<figure class="photo-gallery-figure thumb-figure">
	  <% if edit == true %>
	      <%= link_to '', { :controller => 'photos' , :action => 'photo_editor', :id => photo.id }, 
	                        class: 'image-edit-button' , :remote => true %>
	  <% end %>
	  
	  <%= create_image_url( photo , params[:year] ) %>

	</figure>