<% if @comment.commentable_type == 'Comment' %>

  добавлен ответ на ваш комментарий

  %= @commentable.body %>

<% end %>


<% if @comment.commentable_type == 'Building' %>

  К вашему сооружению <%= @commentable.title %> добавлен комментарий

<% end %>


<% if @comment.commentable_type == 'Article' %>

  

<% end %>

<% if @comment.commentable_type == 'Photo' %>

	К вашей <%= link_to 'фотографии', building_schema_url( @building, @schema, :photo => @commentable.id ) %> добавлен комментарий

<% end %>

<% if @comment.commentable_type == 'Guide' %>

	К вашей <%= link_to 'отметке', building_schema_url( @building, @schema, :guide => @commentable.id ) %> добавлен комментарий

 <% end %>

===============================================

<%= @comment.body %>

